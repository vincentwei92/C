#include <stdio.h>

int brace, bracket, paren;

void in_quote(int c);
void in_comment(void);
void search(int c);

int main()
{
	int c;
	extern int brace, bracket, paren;

	while ((c = getchar()] != EOF) {
		if (c == '/")
	}


	return 0;
}